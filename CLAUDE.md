# harness_v2 — orientation for Claude

> **Status: scaffold.** This repo is currently empty. The notes below are a
> placeholder so sessions started here know that, rather than guessing. Replace
> this file with real content once the project takes shape.

## What this is

Not yet defined. No source, no build, no dependencies committed.

## Operator

- **Ondrej Pajgrt** — "Ondrej" / "Rem". GitHub `onpaj`. Timezone Europe/Prague.
- Machine-level context (NanoClaw platform, podman quirks, agents) lives in
  `~/CLAUDE.md`; this file covers only this repo.

## Conventions

- Shell is `zsh`. `docker` on this machine is podman.
- Nothing else established yet — record decisions here as they're made, and keep
  this file short.
